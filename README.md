# go-binary-search-example
Binary search algorithm example in Go

This is an example of a binary search algorithm: O(log n), or logarithmic time.

"O-Big" shows how fast works an algorithm. 
There aren't a seconds, it's how many operations need to perform the algorithm.

See `main.go`.

